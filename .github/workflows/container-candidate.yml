name: container candidate

on:
  workflow_dispatch:

env:
  CANDIDATE_IMAGE: ghcr.io/crazycoder/crossglyph-testing:candidate-${{ github.sha }}
  BOOTSTRAP_IMAGE: ghcr.io/crazycoder/crossglyph-testing:bootstrap

jobs:
  test:
    permissions:
      contents: read
    uses: ./.github/workflows/test.yml

  candidate:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Sign in to the GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # A source label on the first image links the package to this public
      # repository while GHCR creates it. Seed it without that label, prove
      # anonymous clients cannot read it, then publish the real candidate.
      - name: Bootstrap the private package
        shell: bash
        run: |
          directory="$(mktemp -d)"
          trap 'rm -rf "$directory"' EXIT
          printf 'private CrossGlyph candidate package\n' >"$directory/marker"
          printf 'FROM scratch\nCOPY marker /crossglyph-private-candidate\n' \
            >"$directory/Dockerfile"
          docker buildx build --platform linux/amd64 --push \
            --tag "$BOOTSTRAP_IMAGE" "$directory"

      - name: Refuse a public bootstrap package
        shell: bash
        run: |
          credentials="$(mktemp -d)"
          trap 'rm -rf "$credentials"' EXIT
          if DOCKER_CONFIG="$credentials" \
            docker manifest inspect "$BOOTSTRAP_IMAGE" >/dev/null 2>&1; then
            echo "::error::crossglyph-testing is public; delete the package before publishing a candidate"
            exit 1
          fi

      - name: Push the private candidate
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.CANDIDATE_IMAGE }}
          labels: |
            org.opencontainers.image.source=https://github.com/CrazyCoder/crossglyph
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Refuse a public candidate package
        shell: bash
        run: |
          credentials="$(mktemp -d)"
          trap 'rm -rf "$credentials"' EXIT
          if DOCKER_CONFIG="$credentials" \
            docker manifest inspect "$CANDIDATE_IMAGE" >/dev/null 2>&1; then
            echo "::error::crossglyph-testing became public while linking the source repository"
            exit 1
          fi

      - name: Verify the remote manifest
        shell: bash
        run: |
          docker buildx imagetools inspect "$CANDIDATE_IMAGE" --raw \
            > candidate-manifest.json
          jq -e '
            [.manifests[].platform
             | select(.os == "linux")
             | "\(.os)/\(.architecture)"] as $platforms
            | ($platforms | index("linux/amd64")) != null
              and ($platforms | index("linux/arm64")) != null
          ' candidate-manifest.json

      - name: Smoke test the pulled candidate
        shell: bash
        run: |
          workspace="$(mktemp -d)"
          trap 'docker rm -f crossglyph-candidate >/dev/null 2>&1 || true; rm -rf "$workspace"' EXIT
          mkdir "$workspace/conf"
          cp /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf "$workspace/"
          printf 'fallbacks = no\nsizes = 12\n' >"$workspace/conf/all.conf"

          docker image rm "$CANDIDATE_IMAGE" >/dev/null 2>&1 || true
          docker pull "$CANDIDATE_IMAGE"
          common=(
            --read-only --tmpfs /tmp
            --cap-drop ALL --security-opt no-new-privileges
            --user "$(id -u):$(id -g)"
            --mount "type=bind,source=$workspace,target=/workspace"
          )
          docker run --rm "${common[@]}" "$CANDIDATE_IMAGE" build
          test -n "$(find "$workspace/cpfonts" -name '*.cpfont' -print -quit)"

          docker run -d --name crossglyph-candidate "${common[@]}" \
            -p 127.0.0.1:18000:8000 "$CANDIDATE_IMAGE" preview --no-open
          for _ in {1..30}; do
            if curl --fail --silent --output /dev/null \
              http://127.0.0.1:18000/; then
              exit 0
            fi
            sleep 1
          done
          docker logs crossglyph-candidate
          exit 1
